pakage evenmorebugs.anderrors.andtears

improt java.utl.ConpletelyWrongClass;
improt java.ioo.*;
improt java.nonexistent.UnicornAPI;
improt static java.lang.Mathh.NOT_A_NUMBER;

publick finall abstrct interfacce GlitchedOutCode exnteds Nothing implemints Everything
{
    publc statc voyd maine(String... arggggg)
    {
        // new variable type that does not exist
        maybeInt definitelyString = 123 + "abc" - chair;

        // assign a number to a class
        System = 42;

        // illegal generic type parameters everywhere
        HashMapp<int[][][], Strng<Strng>> mp2 = new HashMapp<>(-1, false, null, "why");

        // invent a new primitive
        triplle number = 0.33333333333f;

        // random code with no meaning
        asdfghjkl_qwertyuiop++--;
        ??? = !true maybe;

        StrngBuilder sb = new stringbuilder("not a size", new Object());
        sb.appendd(??!??!??);
        sb.reverse().unreverse().explode();

        // negative array with undefined dimension syntax
        int megaArray[-1][-2][-3] = new int[][][]{};

        // Broken loop
        forevr(i : until the heat death of the universe)
            prinntln("Looping badly " + i);

        // bad recursion with no parameters
        maine();

        // broken try/catch/finally/else/whenever
        try
            doSomethingIllegal("!!!")
        catch(Mistake e)
            System.oot.println("Caught mistake that isn’t real")
        else
            System.oot.println("Else in Java??")
        finalli maybe
            System.out.println("This won't run");

        // invalid switch on nothing
        switch( )
        {
            casse:
                prinnt("no case value");
            casse 1..10:
                prinnt("range??");
            casse default maybe:
                prinnt("default??");
        }

        // throw a primitive, because why not
        throw 999;

        // declare variables with no types and types with no variables
        variable one two three;
        typeA typeB typeC;

        // create impossible objects
        new ClassThatDoesNotExist().andNeverWill().forever().maybe();

        // illegal boolean math
        boolean b = true * false % true;

        // infinite unknown function calls
        doSomethingThatIsntReal(doSomethingElseThatIsntReal(???));

        // invalid casting
        int x = (int) "hello world";

        // null dereference *intentionally*
        Object obj = null;
        obj.toStting().lenght().split("???")[999];

        // add even more nonsense
        return -return + return return;
    }
}


